---
import katex from "katex";

const { f = "" } = Astro.props;
const html = katex.renderToString(f, {
  throwOnError: false,
  displayMode: true,
});
---

<span set:html={html} />

<!-- moved katex.css to ../layouts/BlogPost.astro -->
